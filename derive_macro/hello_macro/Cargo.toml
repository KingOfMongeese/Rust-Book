[package]
name = "hello_macro"
version = "0.1.0"
edition = "2024"

# cargo add hello_macro_derive --optional --path hello_macro_derive
[dependencies]
hello_macro_derive = { version = "0.1.0", path = "hello_macro_derive", optional = true }

[features]
default = []
derive = ["hello_macro_derive"]
